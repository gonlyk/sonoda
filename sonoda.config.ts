import { Config } from './utils/readConfig'

const config: Config = {
  dbHost: 'localhost',
  dbPort: 5432,
  dbUser: 'postgres',
  dbPassword: '123456',
  dbDatabase: 'sonoda',
}
export default config